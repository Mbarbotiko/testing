var blog = require("../src/blog.js");
var expect = require("chai").expect;

describe("Blog", function() {
  it("Keep track of hw many blog posts a blog maintains on each page", function() {
    
  });

  it("should....", function() {
    expect(function() {
      ;
    }).to.throw(Error);
  });
});
